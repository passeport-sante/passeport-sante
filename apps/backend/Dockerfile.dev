FROM node:20-alpine
RUN apk add --no-cache openssl
WORKDIR /app
COPY package*.json ./
COPY packages ./packages
COPY apps/backend ./apps/backend
RUN npm install
EXPOSE 5000
CMD ["sh", "-lc", "cd /app/apps/backend && npm run dev"]
